services:
  gateway:
    build: .
    ports:
      - "8080:8080"    # REST + admin UI
      - "50051:50051"  # gRPC
    volumes:
      - gateway-data:/data
    env_file: .env
    restart: unless-stopped

volumes:
  gateway-data:
